<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <!-- Gambar Princess Cinderella -->
    <ImageView
        android:id="@+id/imageViewCinderella"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:src="@drawable/princescinderella"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:contentDescription="Gambar Princess Cinderella" />

    <!-- Judul Cerita -->
    <TextView
        android:id="@+id/textViewJudul"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Princess Cinderella"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:paddingTop="10dp"
        android:textColor="#D81B60" />

    <!-- ScrollView untuk teks cerita -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="10dp">

        <TextView
            android:id="@+id/textViewCerita"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:lineSpacingExtra="4dp"
            android:padding="10dp"
            android:textAlignment="viewStart"
            android:textColor="#000000" />
    </ScrollView>

</LinearLayout>
